# DHLR 仓库协作规则

## 版本管理

- `VERSION` 是系统版本号的唯一事实来源。不要在 YAML、Python、Vue 或文档中写死另一份系统版本。
- 版本号遵循 SemVer：`MAJOR.MINOR.PATCH`。
  - `major`：不兼容的协议、配置或用户行为变更。
  - `minor`：向后兼容的新功能。
  - `patch`：向后兼容的修复、性能优化、重构或文档变更。
- 普通提交不自动升级版本。只有用户明确要求“升级版本号并提交代码”或同义表达时才升级。
- 用户未指定升级级别时，根据本次提交的最大影响判断：破坏性变更用 `major`，功能用 `minor`，其余用 `patch`；提交前明确告知所选级别。
- 执行上述请求时：先完成并验证代码，只暂存本次任务相关文件，再运行：

  `python scripts/release.py <major|minor|patch> --commit -m "<提交说明>"`

- 发布脚本只会额外暂存 `VERSION`，并提交调用前已暂存的代码；不会自动加入其他未暂存或未跟踪文件，也不会自动 tag 或 push。
