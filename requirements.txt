# 动火离人安全监测系统 - 依赖包

# 核心框架
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.0.0

# 计算机视觉
opencv-python>=4.9.0
numpy>=1.24.0

# YOLO推理
# ultralytics 仅开发机 pytorch 引擎使用；设备侧走 RKNN，故意不进清单
#ultralytics>=8.1.0
# RKNN 推理（rknnlite）：装仓库根目录的本地轮子 rknn_toolkit_lite2-2.3.2-cp311-*aarch64.whl
# 平台/解释器专属（设备 conda env 为 cp311），无法进本清单，设备侧手动 pip 安装

# 配置管理
pyyaml>=6.0

# WebSocket
websockets>=12.0
aiohttp>=3.9.0

# 开发工具
python-multipart>=0.0.6

# 音频播放
pygame>=2.6.1

# 串口通讯
pyserial>=3.5

# 系统监控（CPU/内存/NPU/磁盘/网络）
psutil>=5.9

# 蓝牙配网（BLE 外设，ai-dhlr-ble.service 依赖；原为开发期手动安装，补回依赖清单）
bless>=0.3.0

#测试框架
pytest>=7.0.0
pytest-asyncio>=0.21.0
